# Apps

Use this folder to track the 8 apps in the facelift program.

Each `app-XX` folder stores app-specific intake, audit, Figma links, rollout status, and QA artifacts. Do not place app source code here.

The authoritative cross-app registry is `apps/registry.json`. Keep it aligned with `docs/APP_ADOPTION_MATRIX.md`.

