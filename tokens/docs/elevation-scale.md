# Elevation Scale

Use elevation deliberately to separate interaction layers. Do not stack heavy shadows across normal content.

| Token | Use |
|---|---|
| none | default flat content |
| sm | subtle card or table region |
| md | drawer, popover, command panel |
| lg | modal or high-priority overlay |

